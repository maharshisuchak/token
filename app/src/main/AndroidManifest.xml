<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartsensesolutions.token">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Landing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SignUpOne" />
        <activity android:name=".activity.SignUpTwo" />
        <activity android:name=".activity.SignUpThree" />
        <activity android:name=".activity.SignUpFour" />
        <activity android:name=".activity.SignUpFive" />
        <activity android:name=".activity.SignUpSix" />
        <activity
            android:name=".activity.Menus"
            android:label="@string/title_activity_menus"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>